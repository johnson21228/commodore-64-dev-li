# PETSCII UI App

Lab 004 introduces a C64-style text UI. It uses cursor placement, colors, and reverse video to make a simple panel.

This lab is the bridge from raw memory facts to user-facing application surfaces.

Core idea:

```text
C64 UI can be text-first.
PETSCII and reverse video are enough to create structure.
```
