# Keyboard Input App

Lab 005 introduces input handling. The program waits for one key and updates the screen based on that key.

Core loop:

```text
draw prompt -> read key -> update response -> repeat
```

This becomes the controller pattern for later C64 applications.
