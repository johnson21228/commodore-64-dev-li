# Sprite Basics App

Lab 006 introduces sprites as a memory/register pipeline.

A sprite appears when:

```text
shape bytes exist in memory
sprite pointer references that memory
VIC-II position registers are set
sprite enable bit is set
sprite color is set
```

This lab keeps the shape small and static so the memory model is clear.
