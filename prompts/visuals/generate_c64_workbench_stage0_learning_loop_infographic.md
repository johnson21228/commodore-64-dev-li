# Generate C64 Workbench Stage 0 Learning Loop Infographic

Use this prompt to regenerate the C64-specific Workbench Stage 0 learning loop infographic.

## Required message

The visual must communicate:

> The C64 Workbench uses one ChatGPT conversation with two roles, a local repo, terminal Capture Back, cc65 builds, and VICE emulator evidence to grow a verified C64 Learning Lab.

## Required structure

Use the general Stage 0 Workbench workflow pattern:

1. Top section: ChatGPT conversation containing both the Workbench role and the II role.
2. Bottom-left section: Terminal window executing Capture Back, `make verify`, lab builds, emulator runs, and `make pack`.
3. Bottom-right section: VICE / C64 emulator evidence surface.
4. Footer: shareable one-sentence summary.

## Required language

Include these lines or close equivalents:

- C64 Workbench: Stage 0 Learning Loop
- One chat window. Two roles. Local repo. Terminal Capture Back. VICE emulator.
- Workbench role governs the work.
- II role reasons, drafts, and packages.
- Terminal executes Capture Back and builds `.prg` files.
- VICE proves the lab runs.
- The repo remembers.

## Current curriculum to show

```text
001 Hello Screen
002 Screen Memory
003 Color Memory
004 PETSCII UI
005 Keyboard Input
006 Sprite Basics
007 SID Tone
008 Memory Pal
```

## Style

Clean, flat, presentation-ready infographic. Use Workbench green, II blue, terminal dark styling, and a C64-style blue emulator screen. Keep text concise and legible.
