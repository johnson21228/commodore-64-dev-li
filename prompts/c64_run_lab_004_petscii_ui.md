# Run Lab 004: PETSCII UI

Apply the latest C64 Learning Lab overlay, then run:

```bash
cd /Users/stevejohnson/Developer/commodore-64-dev-li
make lab004
make lab004-run
```

Confirm that the emulator output matches `labs/004_petscii_ui/expected.md`.
