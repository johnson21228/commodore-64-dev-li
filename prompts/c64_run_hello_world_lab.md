# Prompt: Run C64 Hello World Lab

Use this prompt when continuing the C64 Learning Lab from a fresh LLM session.

## Instruction

Help me run Lab 001: Hello Screen in `/Users/stevejohnson/Developer/commodore-64-dev-li`.

1. Confirm the repo verifies with `make verify`.
2. Build the app with `make lab001`.
3. If VICE is installed and `x64sc` is on path, run `make lab001-run`.
4. If `x64sc` is missing, help me locate/install VICE or open `labs/001_hello_screen/dist/hello_screen.prg` manually.
5. Capture the observed result before calling the lab complete.

Keep the C64 Workbench rule: generated `.prg` artifacts are evidence, not source authority.
