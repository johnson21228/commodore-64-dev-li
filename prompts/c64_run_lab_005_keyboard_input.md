# Run Lab 005: Keyboard Input

Apply the latest C64 Learning Lab overlay, then run:

```bash
cd /Users/stevejohnson/Developer/commodore-64-dev-li
make lab005
make lab005-run
```

Confirm that the emulator output matches `labs/005_keyboard_input/expected.md`.
