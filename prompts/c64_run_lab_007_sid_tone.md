# Run Lab 007: SID Tone

Apply the latest C64 Learning Lab overlay, then run:

```bash
cd /Users/stevejohnson/Developer/commodore-64-dev-li
make lab007
make lab007-run
```

Confirm that the emulator output matches `labs/007_sid_tone/expected.md`.
