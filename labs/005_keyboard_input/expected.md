# Expected: Lab 005 Keyboard Input

The emulator should show command choices:

```text
PRESS H FOR HELLO
PRESS C FOR C64
PRESS Q TO QUIT
```

Press `H` to show a hello response, `C` to show a C64 response, and `Q` to end.
