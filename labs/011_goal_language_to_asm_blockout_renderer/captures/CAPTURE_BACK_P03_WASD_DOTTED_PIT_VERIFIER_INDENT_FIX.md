# Capture Back: P03 WASD Dotted Pit Verifier Indent Fix

The dotted-pit/shared-projection patch was valid, but the report verifier had an indentation error from an automated insertion. This rewrites the two focused verifiers cleanly and preserves the intended checks.
