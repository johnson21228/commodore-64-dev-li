# Source Prompt: C64 Workbench Stage 0 Learning Loop

This source prompt captures the intent behind `docs/visuals/c64_workbench_stage0_learning_loop.png`.

Regenerate a clean infographic that explains the C64 Workbench learning loop as:

```text
ChatGPT conversation -> Workbench role + II role -> terminal Capture Back -> local repo -> cc65 -> VICE -> Capture Back evidence -> re-entry pack
```

The audience is a technically curious friend who may not yet understand Workbench LI. The visual should make the workflow understandable without requiring them to know the history of the product-builder workbench.
