# cc65 Hello Example

This is a starter example for a C64 `.prg` using cc65.

Build:

```bash
cd examples/hello-cc65
make
```

Run in VICE if `x64sc` is installed:

```bash
make run
```

Do not treat this example as validated until the build and emulator output are captured in a session.
